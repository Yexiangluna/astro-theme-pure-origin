---
/**
 * 功能说明：
 * 主页区块组件，用于组织和布局主页的各个内容区域。
 * 提供统一的标题和内容布局样式，支持响应式设计。
 */
import { cn } from 'astro-pure/utils'

// 解构组件属性
const { class: className, title } = Astro.props // className: 自定义类名, title: 区块标题
---

<!-- 区块容器，响应式布局 -->
<section class={cn('flex flex-col gap-y-5 md:flex-row md:gap-y-0', className)}>
  <!-- 区块标题 -->
  <div class='text-xl font-medium md:min-w-36'>
    <h2>{title}</h2>
  </div>
  <!-- 区块内容插槽 -->
  <div class='flex flex-1 flex-col gap-y-3'>
    <slot />
  </div>
</section>
